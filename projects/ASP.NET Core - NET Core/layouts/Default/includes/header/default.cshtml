@* This section deals with the layoutDefault includes for the css styling *@
@*Note: the viewcontent is only used here for the default because thats all we have access to in the _viewstart.cshtml*@
    @if (ViewData["layoutDefault"] != null)
    {		  
        <link rel="stylesheet" href="~/components/layouts/Default/css/min.css" asp-append-version="true" />
    }
@* End of the layoutDefault css styling includes *@